@model LibraryManagmentSystem.Application.DTOs.MemberDto

@{
    ViewBag.Title = "Member Details";
    Layout = "_LayoutWithoutNavBar";
}

<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="~/css/details-member.css" />
</head>
<body>
    <div class="container mt-4">
        <h2 class="mb-4">Member Details</h2>

        <div class="card mb-4">
            <div class="card-header">
                Member Information
            </div>
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-3">First Name</dt>
                    <dd class="col-sm-9">@Model.FirstName</dd>
                    <dt class="col-sm-3">Last Name</dt>
                    <dd class="col-sm-9">@Model.LastName</dd>
                    <dt class="col-sm-3">Email</dt>
                    <dd class="col-sm-9">@Model.Email</dd>
                    <dt class="col-sm-3">Contact Number</dt>
                    <dd class="col-sm-9">@Model.ContactNumber</dd>
                    <dt class="col-sm-3">Membership Date</dt>
                    <dd class="col-sm-9">@Model.MembershipDate.ToString("yyyy-MM-dd")</dd>
                </dl>
            </div>
        </div>

        <div class="d-flex justify-content-start">
            <a href="@Url.Action("Edit", "Member", new { id = Model.Id })" class="btn btn-primary me-2">
                <i class="bi bi-pencil"></i> Edit
            </a>
            <a href="@Url.Action("Index", "Member")" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Back to List
            </a>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
